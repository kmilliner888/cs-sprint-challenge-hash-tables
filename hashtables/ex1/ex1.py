def get_indices_of_item_weights(weights, length, limit):
    # two items whose sum of weights equals the weight limit
    # returns a tuple of integers  (zero, one) where each represents the item weights of the two packages
    # higher one goes in zeroth place
    


    return None
